#include <stdio.h>

int main() {
  int numero; //cria a variável inteira 'numero'
  scanf("%d", &numero); //lê na entrada um número informado pelo usuário e atribui à variável 'numero'

  printf("Hello, World! %d\n", numero); //imprime o número informado ao final do texto

  return 0; //indica sucesso e finaliza a execução do código
}